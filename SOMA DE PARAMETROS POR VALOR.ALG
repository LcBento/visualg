Algoritmo "soma de parametros por valor"
Var
   x, y: inteiro
procedimento soma (a, b: inteiro)
inicio
      a <- a+1
      b <- b+2
      escreval("valor de a = ", a)
      escreval("valor de b = ", b)
      escreval("soma de a+b = ", a+b)
fimprocedimento
Inicio
      X <-4
      Y <-8
      SOMA (x, y)
      escreval ("valor de x = ", x)
      escreval ("valor de y = ", y)
Fimalgoritmo
